# Review Journal

I treated `parity-rel-health-loom` as a project where the smallest useful behavior should still be inspectable.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its reliability focus without claiming live deployment or external usage.

## Cases

- `baseline`: `budget pressure`, score 156, lane `ship`
- `stress`: `failure width`, score 172, lane `ship`
- `edge`: `recovery gap`, score 199, lane `ship`
- `recovery`: `runbook drift`, score 191, lane `ship`
- `stale`: `budget pressure`, score 165, lane `ship`

## Note

This file is intentionally plain so the fixture remains the source of truth.
